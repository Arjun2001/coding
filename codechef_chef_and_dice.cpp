#include <bits/stdc++.h>
using namespace std;

#define long long int ll

void fast() { ios_base::sync_with_stdio(false); cin.tie(NULL); cout.tie(NULL);}

vector<int> arr;
int main() {
    ll tc,n;
    arr.resize(5);
    arr[1] = 20;
    arr[2] = 36;
    arr[3] = 
    arr
    cin>>tc;
    while(tc--) {
        cin>>n;
        

    }
}